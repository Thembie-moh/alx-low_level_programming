#include "main.h"

/**
 * _islower - check if char is lowercase
 * @c: is the char to be cacked
 * Return: 1 if char is lowercae, othersie 0.
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
