#include "main.h"

/**
 * Main - A function that will print an integer 
 * _putchar - The only function to be used to display the integer
 * Return: 0
 */
int print_number(int n)
{
	unsigned int t = n;
	if (n < 0)
	{
	n == -1;
	k = n;
	_putchar('_');
	}
	k /= 10;
	if (k != 0)
	print_num(k);
	_pucthar((unsigned int ) n % 10 + '0');
}
}
