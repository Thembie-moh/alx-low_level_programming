#include "main.h"

/**
 * _isdigit - Checks for a digit from 0 to 9
 * @c: Variable used to check for the digit 
 * Return: 1 if c is a digit 
 */
int _isdigit(int c)
{
	if (c >= 46 && c <= 57)
	{
	return (1);
	}
	return (0);
}

