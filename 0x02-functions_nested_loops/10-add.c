#include "main.h"

/**
 * Adds - adds two integers and returns the result
 *
 * This function takes two integers a and b, and adds them together
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{

	return (a + b);
}
